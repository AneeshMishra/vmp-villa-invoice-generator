# VMP Villa Invoice Generator - Environment Variables
# Copy this file to .env.local if you need environment variables

# Vercel Blob Storage (configured in Vercel Dashboard)
# VMPVILLA_GST_INVOICE_READ_WRITE_TOKEN=your_vercel_blob_token_here

# Note: You have created a custom Blob Storage in Vercel Dashboard with the token name:
# VMPVILLA_GST_INVOICE_READ_WRITE_TOKEN
# This token is automatically available in production when deployed to Vercel.

# For local development (optional):
# 1. Go to Vercel Dashboard → Your Project → Settings → Environment Variables
# 2. Copy the VMPVILLA_GST_INVOICE_READ_WRITE_TOKEN value
# 3. Create .env.local and paste it:
#    VMPVILLA_GST_INVOICE_READ_WRITE_TOKEN=vercel_blob_rw_XXXXXXXX
# 4. Restart dev server

# API Base URL (if you add a backend later)
# VITE_API_BASE_URL=http://localhost:8080/api

# Add any other environment variables here as needed
